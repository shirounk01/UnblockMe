<form method="post" action="{{ path('license_plate_delete', {'id': license_plate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ license_plate.id) }}">
    <button class="btn btn-outline-danger mx-2">Delete</button>
</form>
